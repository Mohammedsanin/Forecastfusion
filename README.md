# Forecastfusion
A Streamlit app for time series forecasting that lets users upload CSV data, configure parameters, and train ANN, RNN, and LSTM models. It supports single- and multi-step forecasting, compares model performance, visualizes predictions, and allows downloading results.
